version: 1
applications:
  - appRoot: meloy-judge-app
    framework: nextjs
    frontend:
      phases:
        preBuild:
          commands:
            - corepack enable
            - corepack prepare pnpm@latest --activate
            - pnpm install --frozen-lockfile
        build:
          commands:
            - pnpm run build
      buildPath: /
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
